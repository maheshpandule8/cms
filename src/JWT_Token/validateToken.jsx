// import jwt from 'jsonwebtoken';

// const SECRET = 'your-256-bit-secret'; // Replace with your actual secret (base64 encoded if required)

// export const ValidateToken = (token) => {
//   try {
//     const decoded = jwt.verify(token, SECRET);
//     return { valid: true, decoded }; // Return decoded payload if valid
//   } catch (error) {
//     return { valid: false, error: error.message }; // Handle invalid token cases
//   }
// };
